

LDAP directory tree (Internet naming): https://www.openldap.org/doc/admin26/intro.html
dc:(domain conponent)



数据以树状结构（DIT）组织，每个条目（Entry）由一个唯一标识（DN, Distinguished Name）和多个属性（Attribute）组成。

一个条目的属性通过LDAP元数据模型（scheme）中的对象类（objectclass）所定义。


























